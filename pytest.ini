[pytest]
pythonpath = .
asyncio_mode = auto
testpaths = tests/
addopts = --cov=specter --cov-report=term-missing
markers =
    slow: marks tests as slow
    network: tests requiring network access
    root: tests requiring root privileges
