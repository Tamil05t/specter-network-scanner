name: CI

on:
  push:
  pull_request:

jobs:
  lint-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install click==8.1.7 rich-click==1.7.4 rich==13.7.1
          pip install -r requirements.txt
          pip install flake8 black mypy
      - name: Lint
        run: |
          flake8 specter tests
          black --check specter tests
          mypy specter
      - name: Test
        run: |
          pytest -m "not slow"

  docker:
    runs-on: ubuntu-latest
    needs: lint-test
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - name: Build image
        run: docker build -t specter:ci .
