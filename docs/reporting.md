# Reporting

The HTML report includes topology, inventories, vulnerabilities, and exploit correlations.
Reports are generated via specter.reporting.html_report.

## Self-Contained Mode

The report is fully self-contained and does not load external CDN assets. Charts and tables
use lightweight inline JavaScript utilities to avoid network dependencies.
